The program is written in java and all you have to do to run is just input this command

I named the file find_input by instead of naming the file find_route also this program can not do the heuristic so don't try that pls. 

find_input.java && java find_input input1.txt Bremen Kassel